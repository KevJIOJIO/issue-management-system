# 快速參考指南

## 📋 目錄
- [常用指令](#常用指令)
- [檔案對照表](#檔案對照表)
- [資料欄位速查](#資料欄位速查)
- [常見操作](#常見操作)

---

## 🚀 常用指令

```bash
# 安裝依賴套件（首次安裝執行即可）
npm install

# 啟動開發伺服器（預設 http://localhost:5173）
npm run dev

# 建置生產版本
npm run build

# 預覽建置結果
npm run preview

# 程式碼檢查
npm run lint
```

---

## 📁 檔案對照表

### 需要修改資料時，請找這些檔案：

| 需求 | 檔案位置 | 說明 |
|------|----------|------|
| **新增/修改專案** | `src/data/projects.js` | 專案代號、CPU/GPU 規格、年份 |
| **新增/修改 Common Issues** | `src/data/issues.js` | 分組的問題列表（顯示在儀表板） |
| **修改單一專案 Issues** | `src/features/issues/data.js` | 單一專案的問題範例、圖表資料 |
| **修改團隊顏色** | `src/features/issues/data.js` | `teamColors` 物件 |
| **修改團隊列表** | `src/features/issues/data.js` | `teams` 陣列 |

### 不需要修改的檔案：

| 檔案類型 | 說明 |
|----------|------|
| `src/components/` | UI 組件，除非要修改介面外觀，否則不需動 |
| `*.config.js` | 設定檔案，通常不需要修改 |
| `src/main.jsx` | 應用程式入口，不需修改 |

---

## 🔍 資料欄位速查

### 專案資料 (projects.js)

```javascript
{
  name: "ZKA",              // 專案代號（必填）
  cpu: [                    // CPU 清單（必填，陣列）
    {
      series: "Intel Raptor Lake",    // CPU 系列
      platform: "Intel Raptor Lake H" // CPU 平台
    }
  ],
  gpu: [                    // GPU 清單（必填，陣列）
    {
      series: "GN22",       // GPU 系列
      model: "GN22-X4"      // GPU 型號
    }
  ],
  year: 2023                // 年份（必填，數字）
}
```

### Issue 資料 (issues.js)

```javascript
{
  project: "ZKA",           // 專案代號（必須存在於 projects.js）
  id: "ISSUE-102",          // Issue ID（格式：ISSUE-數字）
  description: "問題描述",   // 問題描述（字串）
  failureRate: 8,           // 失敗率（0-10 的整數）
  team: "Thermal",          // 負責團隊（必須在 teams 陣列中）
  status: "Open",           // 狀態："Open" 或 "Closed"
  openDate: "2025-09-18"    // 開啟日期（格式：YYYY-MM-DD）
}
```

### 圖表資料 (features/issues/data.js)

```javascript
{
  week: "2025-09-14 ~ 2025-09-20",  // 週次範圍
  EE: 3,                             // EE 團隊當前 Issue 數
  EE_opened: 2,                      // EE 本週新增數
  EE_closed: 1,                      // EE 本週關閉數
  // ... 其他團隊（ME, Thermal, Power, Driver, BIOS, EC）
}
```

---

## ⚙️ 常見操作

### 1. 新增一個專案

**步驟**:
1. 打開 `src/data/projects.js`
2. 在陣列最後新增專案物件
3. 儲存檔案
4. 重新啟動開發伺服器（若正在運行）

**範例**:
```javascript
export const projects = [
  // ... 現有專案
  {
    name: 'ZLE',  // 新專案
    cpu: [{ series: 'Intel Meteor Lake', platform: 'Intel Meteor Lake H' }],
    gpu: [{ series: 'GN25', model: 'GN25-X12' }],
    year: 2025
  }
];
```

### 2. 新增一個 Issue

**步驟**:
1. 打開 `src/data/issues.js`
2. 找到對應的問題群組（或新增群組）
3. 在 `issues` 陣列中新增 Issue 物件
4. 確認 `team` 值存在於團隊列表中
5. 確認 `project` 值存在於 `projects.js` 中

**範例**:
```javascript
{
  group: 'Thermal throttling / fan behavior',
  summary: '...',
  issues: [
    // ... 現有 Issues
    {
      project: 'ZLE',
      id: 'ISSUE-150',
      description: 'New thermal issue',
      failureRate: 7,
      team: 'Thermal',
      status: 'Open',
      openDate: '2025-11-08'
    }
  ]
}
```

### 3. 修改團隊顏色

**步驟**:
1. 打開 `src/features/issues/data.js`
2. 找到 `teamColors` 物件
3. 修改對應團隊的顏色碼（16 進位色碼）

**範例**:
```javascript
export const teamColors = {
  EE: '#2563eb',      // 藍色
  ME: '#10b981',      // 改為綠色（原本是橙色）
  // ...
};
```

### 4. 新增一個團隊

**步驟**:
1. 打開 `src/features/issues/data.js`
2. 在 `teams` 陣列中新增團隊名稱
3. 在 `teamColors` 物件中新增對應顏色
4. 在圖表資料中新增該團隊的欄位

**範例**:
```javascript
// 1. 新增團隊名稱
export const teams = ['EE', 'ME', 'Thermal', 'Power', 'Driver', 'BIOS', 'EC', 'QA'];

// 2. 新增顏色
export const teamColors = {
  // ... 現有團隊
  QA: '#f59e0b'  // 黃色
};

// 3. 圖表資料需包含 QA, QA_opened, QA_closed
```

### 5. 更新圖表資料（新增一週）

**步驟**:
1. 打開 `src/features/issues/data.js`
2. 在 `chartData` 陣列最後新增新的週次物件
3. 確保包含所有團隊的三個數值（current, opened, closed）

**範例**:
```javascript
export const chartData = [
  // ... 現有資料
  {
    week: '2025-11-09 ~ 2025-11-15',
    EE: 6, EE_opened: 4, EE_closed: 2,
    ME: 2, ME_opened: 1, ME_closed: 3,
    Thermal: 4, Thermal_opened: 2, Thermal_closed: 1,
    Power: 1, Power_opened: 0, Power_closed: 2,
    Driver: 4, Driver_opened: 3, Driver_closed: 1,
    BIOS: 2, BIOS_opened: 0, BIOS_closed: 2,
    EC: 3, EC_opened: 2, EC_closed: 1
  }
];
```

### 6. 關閉一個 Issue

**步驟**:
1. 找到該 Issue 在哪個檔案中（`issues.js` 或 `features/issues/data.js`）
2. 將 `status` 從 `"Open"` 改為 `"Closed"`

**範例**:
```javascript
{
  project: 'ZKA',
  id: 'ISSUE-102',
  description: '...',
  failureRate: 8,
  team: 'Thermal',
  status: 'Closed',  // 改為 Closed
  openDate: '2025-09-18'
}
```

---

## 🎨 UI 修改快速參考

### 修改顏色主題

**Tailwind CSS 顏色類別**:
- `bg-blue-50`: 淺藍色背景
- `text-blue-600`: 藍色文字
- `border-blue-300`: 藍色邊框

**自訂顏色**: 在 `tailwind.config.js` 中的 `theme.extend.colors` 新增

### 修改文字

**固定文字**: 直接在對應的 JSX 檔案中搜尋並修改

**範例**: 修改 "Project Overview" 標題
1. 打開 `src/ProjectDashboard.jsx`
2. 搜尋 `"Project Overview"`
3. 修改為想要的文字

---

## ⚠️ 注意事項

### 必須遵守的規則

1. **日期格式**: 統一使用 `YYYY-MM-DD` 格式（如 `2025-11-08`）
2. **Issue ID 格式**: demo先以格式 `ISSUE-` 開頭加數字（如 `ISSUE-102`），實際上follow bugzilla格式
3. **團隊名稱**: 必須與 `teams` 陣列中的名稱完全一致（大小寫敏感）
4. **專案代號**: 在 `projects.js` 中定義後才能在 Issues 中使用
5. **失敗率範圍**: 0-10 的整數

### 常見錯誤

| 錯誤現象 | 可能原因 | 解決方法 |
|----------|----------|----------|
| 專案卡片不顯示 | 資料格式錯誤 | 檢查是否缺少必填欄位 |
| Issue 不顯示 | `project` 不存在 | 確認專案代號在 `projects.js` 中 |
| 圖表無資料 | 團隊名稱不符 | 檢查 `chartData` 中的鍵名是否與 `teams` 一致 |
| 篩選器失效 | `team` 值錯誤 | 確認 Issue 的 `team` 在 `teams` 陣列中 |

---

## 📞 需要協助？

**完整技術文件**: 請參閱 `TECHNICAL_DOCUMENTATION.md`

**問題回報**: 請提供以下資訊
1. 錯誤現象描述
2. 操作步驟
3. 瀏覽器控制台錯誤訊息（F12 → Console）
4. 修改過的檔案名稱

---

**最後更新**: 2025-11-08
